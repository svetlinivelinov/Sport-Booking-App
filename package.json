{
  "name": "sport-booking-app-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Capstone monorepo bootstrap: Next.js web/backend + Expo mobile + shared packages",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "echo Initialize apps/web first",
    "dev:mobile": "echo Initialize apps/mobile first",
    "build": "echo Configure build orchestration after app initialization",
    "lint": "echo Configure lint after app initialization",
    "typecheck": "echo Configure typecheck after app initialization"
  },
  "engines": {
    "node": ">=20"
  }
}